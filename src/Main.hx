package ;
// By some reason the @:keep doesn't work
// We use this import to force the compiler compile things
import gamball.GamBall;

class Main
{
	// Keep main function for Haxe build
	static function main() { }
}